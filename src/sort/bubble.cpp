#include "../include/sort.h"
#include <iostream>

using namespace std;

void sort::bubble(int data[], int len) {

    for (int i = 0; i < len; i++) {
        cout << data[i] << endl;
    }
}